name: CI

on:
  pull_request:
  merge_group:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hooks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@main
        with:
          extra-conf: |
            accept-flake-config = true
            fallback = true

      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Pre-commit hooks
        # Pure flake check -- runs the same hooks without the heavy devShell
        run: nix run nixpkgs#nushell -- .github/nix-build-quiet.nu .#checks.x86_64-linux.git-hooks

  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@main
        with:
          extra-conf: |
            accept-flake-config = true
            fallback = true

      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Test
        run: nix run nixpkgs#nushell -- .github/nix-build-quiet.nu .#moneymentum-test

      - name: Clippy
        run: nix run nixpkgs#nushell -- .github/nix-build-quiet.nu .#moneymentum-clippy

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@main
        with:
          extra-conf: |
            accept-flake-config = true
            fallback = true

      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Lint
        run: nix run nixpkgs#nushell -- .github/nix-build-quiet.nu .#frontend-lint

      - name: Test
        run: nix run nixpkgs#nushell -- .github/nix-build-quiet.nu .#frontend-test
